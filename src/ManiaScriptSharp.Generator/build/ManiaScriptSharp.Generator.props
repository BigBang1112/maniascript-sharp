<Project>

    <ItemGroup>
        <CompilerVisibleProperty Include="MSBuildProjectDirectory" />
        <CompilerVisibleProperty Include="MSBuildProjectName" />
        <CompilerVisibleProperty Include="ManiaScriptOutputDir" />
        <CompilerVisibleProperty Include="ManiaScriptIndentSize" />
        <CompilerVisibleProperty Include="ManiaScriptIndentStyle" />
    </ItemGroup>

    <!-- Manialink XML templates must be visible to the generator as AdditionalFiles so that
         editing them (without touching any .cs file) is tracked as a compiler input: this makes
         both the Roslyn incremental generator cache and MSBuild's up-to-date build check re-run
         the generator when only the XML changes. -->
    <ItemGroup>
        <AdditionalFiles Include="**/*.xml" Exclude="$(DefaultItemExcludes);$(DefaultExcludesInProjectFolder);bin/**;obj/**" />
    </ItemGroup>

</Project>
