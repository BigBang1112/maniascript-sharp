/**
 *  UI module: EndRound server side
 */
#Const Version    "1.0.0"
#Const ScriptName "Libs/Nadeo/Trackmania/Modes/HotSeat/UIModules/EndRound_Server.Script.txt"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Libraries
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Include "Libs/Nadeo/Trackmania/Modes/HotSeat/Consts.Script.txt" as Consts

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Constants
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Const C_Id "UIModules_HotSeat_EndRound"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Functions
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Public
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Show or hide the UI
 *
 *	@param	_Player										The player to update
 *	@param	_Display									True to show
 *																		False to hide
 */
Void SetVisibility(CPlayer _Player, Boolean _Display) {
	if (_Player == Null) return;
	declare netwrite Boolean Net_HotSeat_EndRound_IsVisible for _Player;
	Net_HotSeat_EndRound_IsVisible = _Display;
}

//Start to play the end round animation
Void StartAnimation() {
	declare netwrite Boolean Net_HotSeat_EndRound_AnimationPlaying for Teams[0];
	Net_HotSeat_EndRound_AnimationPlaying = True;
}

//Check if the animation is finished
Boolean IsAnimationPlaying() {
	declare netwrite Boolean Net_HotSeat_EndRound_AnimationPlaying for Teams[0];
	if (Net_HotSeat_EndRound_AnimationPlaying) {
		foreach (Event in UIManager.PendingEvents) {
			if (Event.Type == CUIConfigEvent::EType::OnLayerCustomEvent
					&& Event.CustomEventType == Consts::C_EndRound_Finished) {
				Net_HotSeat_EndRound_AnimationPlaying = False;
			}
		}
	}
	return Net_HotSeat_EndRound_AnimationPlaying;
}

//Only needed if we want to forcefully stop the animation
Void StopAnimation() {
	declare netwrite Boolean Net_HotSeat_EndRound_AnimationPlaying for Teams[0];
	Net_HotSeat_EndRound_AnimationPlaying = False;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Initialize a player
 *
 *	@param	_Player										The player to initialize
 */
Void InitializePlayer(CPlayer _Player) {
	if (_Player == Null) return;
	SetVisibility(_Player, False);
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Unload the library
Void Unload() {
	foreach (Player in AllPlayers) {
		InitializePlayer(Player);
	}
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Load the library
Void Load() {
	Unload();
}