/**
 *	UI module: LiveRanking server side
 */
#Const Version		"1.0.0"
#Const ScriptName	"Libs/Nadeo/Trackmania/Modes/TMWTCommon/UIModules/LiveRanking_Server.Script.txt"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Includes
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Include "Libs/Nadeo/CMGame/Modes/UIModules_Common.Script.txt" as UIModules_Common
#Include "Libs/Nadeo/Trackmania/Modes/TMWTCommon/UIModules/LiveRanking_Common.Script.txt" as UIModules_LiveRanking_Common

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Constants
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Const C_DefaultVisibility False

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Functions
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Get the id of the UI module
Text GetId() {
	return UIModules_LiveRanking_Common::C_Id;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Get the configuration of the UI module
UIModules_Common::K_ModuleConfig GetConfig() {
	return UIModules_LiveRanking_Common::C_UIModuleConfig;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Start a new race
Void ResetRace() {
	declare netwrite Integer Net_TMWTCommon_LiveRanking_RankingUpdate for Teams[0] = 0;
	declare netwrite Integer Net_TMWTCommon_LiveRanking_RaceIndex for Teams[0] = 0;
	declare netwrite Text[] Net_TMWTCommon_LiveRanking_FinishedRacePlayers for Teams[0] = [];
	Net_TMWTCommon_LiveRanking_RankingUpdate += 1;
	Net_TMWTCommon_LiveRanking_RaceIndex += 1;
	Net_TMWTCommon_LiveRanking_FinishedRacePlayers = [];
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// A player crossed the finish line
Void PlayerFinishRace(CSmPlayer _Player) {
	if (_Player == Null || _Player.User == Null) return;

	declare netwrite Integer Net_TMWTCommon_LiveRanking_RankingUpdate for Teams[0] = 0;
	declare netwrite Text[] Net_TMWTCommon_LiveRanking_FinishedRacePlayers for Teams[0] = [];
	if (!Net_TMWTCommon_LiveRanking_FinishedRacePlayers.exists(_Player.User.WebServicesUserId)) {
		Net_TMWTCommon_LiveRanking_RankingUpdate += 1;
		Net_TMWTCommon_LiveRanking_FinishedRacePlayers.add(_Player.User.WebServicesUserId);
	}
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Force an update of the live ranking on the client
Void ForceUpdate() {
	declare netwrite Integer Net_TMWTCommon_LiveRanking_RankingUpdate for Teams[0] = 0;
	Net_TMWTCommon_LiveRanking_RankingUpdate += 1;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Show or hide the UI
 *
 *	@param	_Player										The player to update
 *	@param	_Display									True to show
 *																		False to hide
 */
Void SetVisibility(CPlayer _Player, Boolean _Display) {
	if (_Player == Null) return;
	declare CUIConfig UI <=> UIManager.GetUI(_Player);
	if (UI == Null) return;

	declare netwrite Boolean Net_TMWTCommon_LiveRanking_IsVisible for UI = C_DefaultVisibility;
	Net_TMWTCommon_LiveRanking_IsVisible = _Display;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Initialize a player
 *
 *	@param	_Player										The player to initialize
 */
Void InitializePlayer(CPlayer _Player) {
	if (_Player == Null) return;

	SetVisibility(_Player, C_DefaultVisibility);
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Unload the library
Void Unload() {
	ResetRace();
	foreach (Player in AllPlayers) {
		InitializePlayer(Player);
	}
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Load the library
Void Load() {
	Unload();
}