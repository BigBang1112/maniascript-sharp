/**
 * UI module: Markers server side
 */
#Const Version "1.0.0"
#Const ScriptName "Libs/Nadeo/Trackmania/Modes/TMWTMatchmaking/UIModules/Markers_Server.Script.txt"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Includes
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Include "Libs/Nadeo/CMGame/Modes/UIModules_Common.Script.txt" as UIModules_Common
#Include "Libs/Nadeo/Trackmania/Modes/TMWTMatchmaking/UIModules/Markers_Common.Script.txt" as UIModules_Markers_Common

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Constants
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Const C_DefaultVisibility False

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Functions
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Get the id of the UI module
Text GetId() {
	return UIModules_Markers_Common::C_Id;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Get the configuration of the UI module
UIModules_Common::K_ModuleConfig GetConfig() {
	return UIModules_Markers_Common::C_UIModuleConfig;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Send the frame id of the visible markers
Void SetMarkerFrameId(Integer _MarkerFrameId, Text _AccountId) {
	declare netwrite Text[Integer] Net_TMWTMatchmakingMarkers_MarkerFrameIds for Teams[0] = [];
	declare netwrite Integer Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate for Teams[0] = 0;
	Net_TMWTMatchmakingMarkers_MarkerFrameIds[_MarkerFrameId] = _AccountId;
	Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate += 1;
}
Void RemoveMarkerFrameId(Integer _MarkerFrameId) {
	declare netwrite Text[Integer] Net_TMWTMatchmakingMarkers_MarkerFrameIds for Teams[0] = [];
	declare netwrite Integer Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate for Teams[0] = 0;
	Net_TMWTMatchmakingMarkers_MarkerFrameIds.removekey(_MarkerFrameId);
	Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate += 1;
}
Void ResetMarkerFrameIds() {
	declare netwrite Text[Integer] Net_TMWTMatchmakingMarkers_MarkerFrameIds for Teams[0] = [];
	declare netwrite Integer Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate for Teams[0] = 0;
	Net_TMWTMatchmakingMarkers_MarkerFrameIds = [];
	Net_TMWTMatchmakingMarkers_MarkerFrameIdsUpdate += 1;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Force the initialization of the markers on the client
Void Initialize() {
	declare netwrite Integer Net_TMWTMatchmakingMarkers_InitializeUpdate for Teams[0] = 0;
	Net_TMWTMatchmakingMarkers_InitializeUpdate = Now;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Show or hide the UI
 *
 *	@param	_Player										The player to update
 *	@param	_Display									True to show
 *																		False to hide
 */
Void SetVisibility(CPlayer _Player, Boolean _Display) {
	if (_Player == Null) return;
	declare CUIConfig UI <=> UIManager.GetUI(_Player);
	if (UI == Null) return;

	declare netwrite Boolean Net_TMWTMatchmakingMarkers_IsVisible for UI = C_DefaultVisibility;
	Net_TMWTMatchmakingMarkers_IsVisible = _Display;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/** Initialize a player
 *
 *	@param	_Player										The player to initialize
 */
Void InitializePlayer(CPlayer _Player) {
	if (_Player == Null) return;

	SetVisibility(_Player, C_DefaultVisibility);
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Unload the library
Void Unload() {
	Initialize();
	ResetMarkerFrameIds();

	foreach (Player in AllPlayers) {
		InitializePlayer(Player);
	}
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Load the library
Void Load() {
	Unload();
}