/**
 *	UI module: PickAndBan common
 */
#Const Version		"1.0.0"
#Const ScriptName	"Libs/Nadeo/TMGame/Modes/Base/UIModules/PickAndBan_Common.Script.txt"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Includes
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Include "TextLib" as TL
#Include "Libs/Nadeo/CMGame/Modes/UIModules_Common.Script.txt" as UIModules_Common

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Structures
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Struct K_Step {
	Integer Team;
	Text Action;
	Text MapUid;
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Constants
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
#Const C_Id "UIModule_TMGame_PickAndBan"
#Const C_LayerType CUILayer::EUILayerType::Normal
#Const C_UIModuleConfig UIModules_Common::K_ModuleConfig {
	Id = "TMGame_PickAndBan", //< Do not modify this Id once in production. It is used by the xmlrpc protocole and would break compatibility.
	IsCustomizable = True,
	Properties = UIModules_Common::K_ModuleProperties {
		Position = <0., 0.>,
		Scale = 1.,
		Visible = True
	}
}

#Const C_Action_Pick "pick"
#Const C_Action_Ban "ban"
#Const C_Action_RandomPick "randompick"
#Const C_Actions ["pick", "ban", "randompick"]

#Const C_Style_Background "Background"
#Const C_Style_TopLeftLogo "TopLeftLogo"
#Const C_Style_TopRightLogo "TopRightLogo"
#Const C_Style_BottomLogo "BottomLogo"

#Const C_Event_TrackSelected "TMGame_PickAndBan_TrackSelected"
#Const C_Event_TrackPreSelected "TMGame_PickAndBan_TrackPreSelected"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Function
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
/// Action values are case insensitive
Text FormatAction(Text _Action) {
	return TL::ToLowerCase(_Action);
}

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// Check if the action is a valid one
Boolean IsValidAction(Text _Action) {
	return C_Actions.exists(_Action);
}