/**
 * Manage the DecoImage (checkpoints, bleachers, signs, ...) client side
 */
#Const Version    "1.0.0"
#Const ScriptName "Libs/Nadeo/TMGame/Modes/DecoImage_Client.Script.txt"

// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //
// MARK: Functions
// ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ //

//  Get the club id
Integer GetClubId() {
	declare netread Integer Net_TMGame_DecoImage_ClubId for Teams[0] = 0;
	return Net_TMGame_DecoImage_ClubId;
}

//  Get the club name
Text GetClubName() {
	declare netread Text Net_TMGame_DecoImage_ClubName for Teams[0] = "";
	return Net_TMGame_DecoImage_ClubName;
}